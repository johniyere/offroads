query dashboard {
  me {,
    id,
    name,
    createdRoutes {
      id
      name,
      createdAt,
      creator {
        id,
        name
      },
      avgRating
    },
    followers {
      id,
      name
    },
    following {
      id,
      name
    },
    bookmarkedTrails {
      id,
      name,
      createdAt,
      creator {
        id,
        name
      },
      avgRating
    },
    runs {
      id,
      title,
      comment,
      route {
        id, name
      },
      time,
      createdAt
    }
  }
}